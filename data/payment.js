// for data related to payment

const getPriceForGame = async (game_id) => {
    
}

const addToGamersProfile = async(game_id)=>{
    // add the provided game_id to gamer's profile
}

module.exports = {
    getPriceForGame,
    addToGamersProfile
}