// const gameroutes = require('./')  import routes here

const constructorMethod = (app) =>{
    // app.use()

    // app.use('*', (req,res) => {
    //     res.status(404).render()
    // });
};

module.exports = constructorMethod;